#include <stdio.h>
#include <string.h>

int main(){
	double val1, val2, result;
	char operator[10];

	printf("Ingresa la operación: ");

	if(strcmp(operator, "root") == 0){
		result = val1 * val1;
		printf("= %lf", result);
	}

}
